package model.repository;

import model.entities.ProductModel;

public class ProductRepositoryImpl implements ProductRepository {
    @Override
    public ProductModel findByUUID(String uuid) {
        return null;
    }
}
